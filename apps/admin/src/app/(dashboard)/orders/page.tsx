import { ComingNext } from "@/components/coming-next";

export default function OrdersPage() {
  return (
    <ComingNext
      title="Orders"
      note="Order creation, confirmation, credit-limit checks, PDF export and the status timeline are all live in the API — this screen adds the order builder and timeline view next."
    />
  );
}
