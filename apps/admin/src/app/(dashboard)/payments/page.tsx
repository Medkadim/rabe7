import { ComingNext } from "@/components/coming-next";

export default function PaymentsPage() {
  return (
    <ComingNext
      title="Payments"
      note="Recording cash, transfer, cheque and credit payments against invoices is live in the API — this screen adds the receipt list and record-payment form next."
    />
  );
}
