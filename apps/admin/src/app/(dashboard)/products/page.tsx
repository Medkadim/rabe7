import { ComingNext } from "@/components/coming-next";

export default function ProductsPage() {
  return (
    <ComingNext
      title="Products"
      note="The Products API (categories, brands, price tiers, customer-specific pricing) is complete and live — this screen wires up list, search and the create form next, the same way the Customers page does."
    />
  );
}
